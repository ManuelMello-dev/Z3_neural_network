# Install an environment-appropriate PyTorch build.
# CPU-only default shown here; for CUDA builds, follow https://pytorch.org/get-started/locally/
torch>=2.2
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
numpy>=1.24
